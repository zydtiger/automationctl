Audit this repository and report, as JSON with a single `result` string field:

- open pull requests with no activity in the last seven days
- branches merged upstream but not deleted locally
- any failing checks on the default branch

Do not modify the working tree. Keep the report under twenty lines.
